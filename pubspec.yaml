name: shelf_core_example
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.2

# Add regular dependencies here.
dependencies:
  build_runner: ^2.4.6
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_router_generator: ^1.0.6
  shelf_static: ^1.1.2
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
